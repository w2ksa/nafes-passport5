# 🔥 إعداد Firebase - جواز نافس

## ✅ ما تم إنجازه

تم ربط المشروع بنجاح بـ **Firebase** مع قاعدة بيانات **Firestore** وإعداد **Hosting** للحصول على دومين مجاني.

### الملفات التي تم إنشاؤها:

1. **`client/src/lib/firebaseConfig.ts`** - إعدادات Firebase
2. **`client/src/lib/firestoreService.ts`** - خدمة قاعدة البيانات (CRUD)
3. **`client/src/components/DatabaseStatus.tsx`** - مكون للتحقق من الاتصال
4. **`client/src/hooks/useStudents.ts`** - Hook لجلب بيانات الطلاب
5. **`firebase.json`** - إعدادات Firebase Hosting
6. **`firestore.rules`** - قواعد أمان قاعدة البيانات
7. **`firestore.indexes.json`** - فهارس قاعدة البيانات
8. **`FIREBASE_SETUP.md`** - دليل الإعداد التفصيلي

### التحديثات:

- ✅ تحديث `AdminDashboard` لاستخدام قاعدة البيانات
- ✅ تحديث `Home` لاستخدام قاعدة البيانات
- ✅ تحديث `StudentDashboard` لاستخدام قاعدة البيانات
- ✅ تحديث `Leaderboard` لاستخدام قاعدة البيانات

## 🚀 الخطوات التالية

### 1. إنشاء مشروع Firebase

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. انقر على **"Add project"** أو **"إضافة مشروع"**
3. أدخل اسم المشروع: `nafes-passport`
4. اتبع الخطوات لإكمال الإنشاء

### 2. إعداد قاعدة بيانات Firestore

1. في Firebase Console، اذهب إلى **Firestore Database**
2. انقر على **Create database**
3. اختر **Start in test mode** (للبداية السريعة)
4. اختر موقع قاعدة البيانات (مثلاً: `us-central1`)
5. انقر على **Enable**

### 3. الحصول على معلومات الإعداد

1. في Firebase Console، اذهب إلى **Project Settings** (⚙️)
2. انتقل إلى تبويب **General**
3. في قسم **Your apps**، انقر على أيقونة الويب `</>`
4. إذا لم يكن لديك تطبيق ويب، انقر على **Add app** واختر **Web**
5. انسخ معلومات الإعداد (config)

### 4. إعداد ملف `.env`

1. أنشئ ملف `.env` في المجلد الرئيسي للمشروع
2. أضف القيم التالية (استبدل بالقيم من Firebase):

```env
VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
VITE_FIREBASE_APP_ID=your-app-id
VITE_FIREBASE_MEASUREMENT_ID=your-measurement-id
```

### 5. نشر قواعد الأمان

```bash
# تسجيل الدخول
firebase login

# ربط المشروع
firebase use --add
# اختر مشروعك من القائمة

# نشر القواعد
firebase deploy --only firestore:rules
```

### 6. نشر المشروع على Firebase Hosting (الدومين المجاني)

```bash
# بناء المشروع
pnpm build

# نشر المشروع
firebase deploy --only hosting
```

بعد النشر، ستحصل على دومين مجاني مثل:
- `https://your-project-id.web.app`
- `https://your-project-id.firebaseapp.com`

## ✅ التحقق من الاتصال

1. شغّل المشروع محلياً:
```bash
pnpm dev
```

2. افتح لوحة التحكم (Admin Dashboard)
3. يجب أن ترى رسالة **"✅ متصل بقاعدة البيانات"** في أعلى الصفحة

## 📝 ملاحظات مهمة

### قواعد الأمان

القواعد الحالية تسمح بالقراءة للجميع والكتابة حتى 2026. يجب تحديثها لاحقاً:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /students/{studentId} {
      allow read: if true;
      allow write: if request.auth != null; // يتطلب مصادقة
    }
  }
}
```

### البيانات التجريبية

- البيانات الحالية في `data.ts` هي بيانات تجريبية
- بعد ربط Firebase، سيتم جلب البيانات من قاعدة البيانات
- يمكنك إضافة بيانات تجريبية من لوحة التحكم

### الدومين المجاني

- Firebase Hosting يوفر دومين مجاني دائماً
- يمكنك ربط دومين مخصص لاحقاً من Firebase Console
- الدومين المجاني متاح فوراً بعد النشر

## 🔧 استكشاف الأخطاء

### خطأ: "خطأ في الاتصال بقاعدة البيانات"

**الحل:**
1. تأكد من إضافة جميع متغيرات البيئة في ملف `.env`
2. تأكد من أن قاعدة البيانات Firestore مفعّلة
3. تحقق من قواعد الأمان في Firebase Console
4. أعد تشغيل خادم التطوير بعد إضافة `.env`

### خطأ: "Permission denied"

**الحل:**
- تحقق من قواعد الأمان في Firestore
- تأكد من نشر القواعد باستخدام `firebase deploy --only firestore:rules`

### خطأ: "Firebase app not initialized"

**الحل:**
- تأكد من وجود ملف `.env` مع جميع المتغيرات المطلوبة
- أعد تشغيل خادم التطوير بعد إضافة `.env`

## 📚 روابط مفيدة

- [Firebase Console](https://console.firebase.google.com/)
- [Firebase Documentation](https://firebase.google.com/docs)
- [Firestore Documentation](https://firebase.google.com/docs/firestore)
- [Firebase Hosting](https://firebase.google.com/docs/hosting)

---

**تم الإعداد بنجاح! 🎉**


