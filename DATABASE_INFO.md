# معلومات قاعدة البيانات 📊

**آخر تحديث:** 7 فبراير 2026

---

## 🔥 Firebase Firestore Database

### المعلومات الأساسية

| الحقل | القيمة |
|-------|--------|
| **اسم قاعدة البيانات** | `(default)` |
| **النوع** | Standard |
| **الموقع** | nam5 (North America) |
| **الوضع** | Native |
| **معرف المشروع** | nafes-passport5 |

---

## 📦 المجموعات (Collections)

### 1. `students`
**الوصف:** بيانات الطلاب

**الحقول:**
- `id` - معرف الطالب
- `name` - اسم الطالب
- `grade` - الصف (3 أو 6)
- `points` - النقاط (arabic, math, science, morningAssembly, nafesExams)
- `totalPoints` - مجموع النقاط
- `rank` - الرتبة الحالية
- `stamps` - الأختام (silver, gold, diamond)
- `viewCount` - عدد المشاهدات
- `comments` - التعليقات (مصفوفة)
- `avatar` - رابط الصورة الشخصية
- `createdAt` - تاريخ الإنشاء
- `updatedAt` - تاريخ آخر تحديث

**عدد المستندات:** ~135 طالب

---

### 2. `change_logs` (جديدة)
**الوصف:** سجلات التغييرات

**الحقول:**
- `id` - معرف السجل
- `timestamp` - تاريخ ووقت التغيير
- `action` - نوع العملية (add/update/delete/bulk_update/restore)
- `studentId` - معرف الطالب
- `studentName` - اسم الطالب
- `changes` - قائمة التغييرات التفصيلية
- `snapshotBefore` - نسخة كاملة من البيانات قبل التعديل
- `bulkOperation` - معلومات العملية الجماعية (إن وجدت)
- `restoredFrom` - معرف السجل الذي تم الاستعادة منه (إن وجد)

**عدد المستندات:** متغير (حسب الاستخدام)

---

## 🔒 قواعد الأمان الحالية

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قاعدة بيانات الطلاب
    match /students/{studentId} {
      allow read: if true;
      allow write: if request.time < timestamp.date(2030, 12, 31);
    }
    
    // سجلات التغييرات
    match /change_logs/{logId} {
      allow read: if true;
      allow write: if request.time < timestamp.date(2030, 12, 31);
    }
    
    // منع الوصول لأي شيء آخر
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

**ملاحظة:** القواعد محدثة محلياً لتنتهي في 2030، لكن تحتاج نشر على Firebase Console.

---

## 📈 الإحصائيات الحالية

**من الصورة (Last 7 days):**
- **القراءات:** 814 total
- **الكتابات:** 2 total
- **الحذف:** 0 total

**التاريخ:** Jan 31 - Feb 4

---

## 🌍 معلومات الموقع

**Location:** nam5
- المنطقة: North America (أمريكا الشمالية)
- السيرفرات: موزعة جغرافياً للسرعة

---

## 🔗 الروابط السريعة

### Firebase Console:
```
https://console.firebase.google.com/project/nafes-passport5/firestore/databases/-default-/data
```

### قواعد الأمان:
```
https://console.firebase.google.com/project/nafes-passport5/firestore/rules
```

### الفهرسة (Indexes):
```
https://console.firebase.google.com/project/nafes-passport5/firestore/indexes
```

### الاستخدام (Usage):
```
https://console.firebase.google.com/project/nafes-passport5/usage
```

---

## 💾 النسخ الاحتياطي

**الحالة:** تلقائي عبر Firebase
**المدة:** مدى الحياة (طالما المشروع موجود)
**الموقع:** سحابة Google

---

## 📊 الحدود المجانية (Spark Plan)

| الخدمة | الحد المجاني | الاستخدام الحالي | النسبة |
|--------|-------------|------------------|--------|
| التخزين | 1 GB | ~5 MB | 0.5% |
| القراءات | 50,000 / يوم | ~116 / يوم | 0.2% |
| الكتابات | 20,000 / يوم | ~0.3 / يوم | 0.001% |
| الحذف | 20,000 / يوم | ~0 / يوم | 0% |

**الحالة:** ✅ ضمن الحدود بشكل كبير

---

## ⚙️ إعدادات الاتصال في الكود

**الملف:** `client/src/lib/firebaseConfig.ts`

```typescript
import { getFirestore } from "firebase/firestore";

// تهيئة Firestore
const db = getFirestore(app);  // يستخدم (default) تلقائياً
```

**ملاحظة:** 
- لا نحتاج تحديد اسم قاعدة البيانات لأن `(default)` هو الافتراضي
- إذا كان لديك قواعد بيانات إضافية، يمكن تحديدها: `getFirestore(app, 'database-name')`

---

## 📝 ملاحظات مهمة

1. **اسم قاعدة البيانات الافتراضي:** `(default)` - لا يمكن تغييره
2. **الموقع الجغرافي:** `nam5` - تم تحديده عند الإنشاء ولا يمكن تغييره
3. **النوع:** Native - هذا هو الوضع الموصى به
4. **الطبعة:** Standard - مناسبة للاستخدام العادي

---

## 🔄 التحديثات المستقبلية

### المخطط:
- [ ] إضافة فهارس (Indexes) لتحسين الأداء
- [ ] إضافة مجموعة `users` للمعلمين (مستقبلاً)
- [ ] إضافة مجموعة `settings` للإعدادات العامة
- [ ] تفعيل النسخ الاحتياطي المجدول

---

```
╔════════════════════════════════════════════════════╗
║                                                    ║
║   📊 قاعدة البيانات: (default)                    ║
║   📍 الموقع: nam5                                 ║
║   ✅ الحالة: نشطة وتعمل بشكل ممتاز                ║
║   📈 الاستخدام: أقل من 1% من الحدود المجانية     ║
║                                                    ║
╚════════════════════════════════════════════════════╝
```

---

**تم التحديث:** 7 فبراير 2026  
**المصدر:** Firebase Console Screenshot  
**الحالة:** ✅ موثق ومحدث
