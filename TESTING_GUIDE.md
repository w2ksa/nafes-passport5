# دليل اختبار النظام 🧪

دليل شامل لاختبار نظام جواز نافس للتأكد من جاهزيته للاستخدام.

---

## 📋 جدول المحتويات

1. [الاختبارات المتاحة](#الاختبارات-المتاحة)
2. [كيفية تشغيل الاختبارات](#كيفية-تشغيل-الاختبارات)
3. [شرح الاختبارات](#شرح-الاختبارات)
4. [قراءة النتائج](#قراءة-النتائج)
5. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🎯 الاختبارات المتاحة

### 1. اختبار الدخول المتزامن (Concurrent Access Test)

يحاكي دخول عدة أجهزة في نفس الوقت للموقع.

**ما يتم اختباره:**
- ✅ سرعة استجابة الموقع
- ✅ القدرة على تحمل الضغط
- ✅ تحميل الملفات الثابتة (صور، JS، CSS)
- ✅ استقرار الخادم

**الأمر:**
```bash
npm run test:concurrent
```

أو مباشرة:
```bash
node scripts/testConcurrentAccess.js
```

---

### 2. اختبار التزامن في Firestore

يختبر قاعدة البيانات عند دخول عدة مستخدمين في نفس الوقت.

**ما يتم اختباره:**
- ✅ القراءة المتزامنة
- ✅ الكتابة المتزامنة
- ✅ قواعد الأمان
- ✅ Real-time Updates

**الأمر:**
```bash
npm run test:firestore
```

أو مباشرة:
```bash
node scripts/testFirestoreSync.mjs
```

---

### 3. اختبار شامل

يشغل جميع الاختبارات بالترتيب.

**الأمر:**
```bash
npm run test:all
```

---

## 🚀 كيفية تشغيل الاختبارات

### المتطلبات

1. **Node.js** (الإصدار 18 أو أحدث)
2. **pnpm** (مُثبَّت)
3. **اتصال بالإنترنت** (للوصول إلى Firebase)

### خطوات التشغيل

#### الطريقة 1: استخدام npm scripts

```bash
# 1. افتح Terminal
cd /path/to/nafes-passport5

# 2. تأكد من تثبيت المكتبات
pnpm install

# 3. شغّل الاختبار المطلوب
npm run test:concurrent    # اختبار الدخول المتزامن
npm run test:firestore     # اختبار Firestore
npm run test:all          # جميع الاختبارات
```

#### الطريقة 2: تشغيل مباشر

```bash
# اختبار الدخول المتزامن
node scripts/testConcurrentAccess.js

# اختبار Firestore
node scripts/testFirestoreSync.mjs
```

---

## 📚 شرح الاختبارات

### اختبار الدخول المتزامن

#### ما يحدث في الاختبار؟

1. **المرحلة 1:** محاكاة 5 أجهزة
   - يتم إرسال 5 طلبات HTTP في نفس الوقت
   - قياس وقت الاستجابة لكل طلب
   - التأكد من نجاح جميع الطلبات

2. **المرحلة 2:** محاكاة 10 أجهزة
   - يتم إرسال 10 طلبات HTTP في نفس الوقت
   - قياس التحسن في الأداء (بسبب Cache)

3. **المرحلة 3:** محاكاة 20 جهاز
   - اختبار القدرة على تحمل ضغط أكبر
   - التأكد من عدم وجود timeout errors

4. **المرحلة 4:** اختبار الملفات الثابتة
   - التأكد من تحميل الصور
   - قياس سرعة التحميل

#### مثال على النتائج

```
═══════════════════════════════════════════
  اختبار الدخول المتزامن لنظام جواز نافس
═══════════════════════════════════════════

⚡ عدد الأجهزة المحاكاة: 5
🌐 الموقع المستهدف: https://nafes-passport.web.app

🚀 بدء الاختبار...

✓ [جهاز 1] نجح - الحالة: 200 - الوقت: 521ms - الحجم: 359.17KB
✓ [جهاز 2] نجح - الحالة: 200 - الوقت: 500ms - الحجم: 359.17KB
✓ [جهاز 3] نجح - الحالة: 200 - الوقت: 500ms - الحجم: 359.17KB
✓ [جهاز 4] نجح - الحالة: 200 - الوقت: 501ms - الحجم: 359.17KB
✓ [جهاز 5] نجح - الحالة: 200 - الوقت: 500ms - الحجم: 359.17KB

✓ نجح: 5/5
✗ فشل: 0/5
⏱ الوقت الكلي: 528ms
⏱ متوسط وقت الاستجابة: 504.40ms
📊 معدل النجاح: 100.0%

✅ ممتاز! النظام يتحمل الضغط بشكل ممتاز
```

---

### اختبار Firestore

#### ما يحدث في الاختبار؟

1. **اختبار القراءة المتزامنة (5 قراء)**
   - 5 مستخدمين يقرؤون البيانات في نفس الوقت
   - قياس وقت الاستجابة
   - التأكد من تطابق البيانات

2. **اختبار القراءة المتزامنة (10 قراء)**
   - 10 مستخدمين يقرؤون البيانات في نفس الوقت
   - قياس التحسن في الأداء

3. **اختبار الكتابة المتزامنة**
   - 5 مستخدمين يكتبون بيانات في نفس الوقت
   - التحقق من قواعد الأمان
   - التأكد من عدم حدوث data conflicts

4. **اختبار Real-time Listeners**
   - إنشاء 3 مستمعين للتحديثات الفورية
   - التأكد من استلام جميع المستمعين للتحديثات

#### مثال على النتائج

```
═══════════════════════════════════════════
  اختبار القراءة المتزامنة من Firestore
═══════════════════════════════════════════

📖 عدد القراء المتزامنين: 5

✓ [قارئ 1] نجح - عدد الطلاب: 135 - الوقت: 943ms
✓ [قارئ 2] نجح - عدد الطلاب: 135 - الوقت: 943ms
✓ [قارئ 3] نجح - عدد الطلاب: 135 - الوقت: 943ms
✓ [قارئ 4] نجح - عدد الطلاب: 135 - الوقت: 943ms
✓ [قارئ 5] نجح - عدد الطلاب: 135 - الوقت: 943ms

✓ نجح: 5/5
⏱ متوسط وقت القراءة: 943.00ms
📊 متوسط عدد الطلاب المقروءة: 135
📊 معدل النجاح: 100.0%

✅ ممتاز! القراءة المتزامنة تعمل بشكل ممتاز
```

---

## 📊 قراءة النتائج

### رموز الحالة

| الرمز | المعنى |
|------|---------|
| ✅ | نجح الاختبار |
| ✗ | فشل الاختبار |
| ⚠️ | تحذير - يحتاج انتباه |
| 📊 | إحصائيات |
| ⏱ | وقت الاستجابة |

### معايير النجاح

#### اختبار الدخول المتزامن

| المعيار | القيمة المثالية | مقبول |
|---------|----------------|--------|
| معدل النجاح | 100% | > 95% |
| متوسط الوقت | < 500ms | < 1000ms |
| الوقت الكلي | < 1000ms | < 2000ms |

#### اختبار Firestore

| المعيار | القيمة المثالية | مقبول |
|---------|----------------|--------|
| معدل النجاح (قراءة) | 100% | > 98% |
| متوسط وقت القراءة | < 500ms | < 1500ms |
| عدد الطلاب | يطابق قاعدة البيانات | - |

### تفسير الأخطاء الشائعة

#### 1. PERMISSION DENIED في اختبار الكتابة

```
✗ [كاتب 1] فشل - 7 PERMISSION_DENIED
```

**المعنى:** قواعد الأمان تعمل بشكل صحيح ✅  
**هل هذا خطأ؟** لا! هذا دليل على أن قواعد الأمان تحمي قاعدة البيانات  
**ماذا تفعل؟** لا شيء - النظام يعمل كما ينبغي

#### 2. Timeout Error

```
✗ [جهاز 1] فشل - timeout of 10000ms exceeded
```

**المعنى:** استغرق الطلب أكثر من 10 ثواني  
**الحل:**
- تحقق من اتصال الإنترنت
- تحقق من حالة Firebase Hosting
- أعد المحاولة

#### 3. 404 Not Found

```
✗ [جهاز 1] فشل - Request failed with status code 404
```

**المعنى:** الصفحة أو الملف غير موجود  
**الحل:**
- تأكد من نشر الموقع على Firebase
- تحقق من رابط الموقع في السكريبت

---

## 🔧 استكشاف الأخطاء

### المشكلة: "Cannot find module 'axios'"

**الحل:**
```bash
pnpm install axios
```

### المشكلة: "Firebase not initialized"

**الحل:**
تأكد من ملف `.env`:
```bash
# تحقق من وجود الملف
ls -la .env

# تأكد من المحتوى
cat .env
```

### المشكلة: "ECONNREFUSED" أو "Network Error"

**الحل:**
- تحقق من اتصال الإنترنت
- تأكد من عدم حظر Firebase في الشبكة
- جرّب VPN إذا لزم الأمر

### المشكلة: جميع الاختبارات تفشل

**الحل:**
```bash
# 1. تحقق من حالة الموقع
curl -I https://nafes-passport.web.app

# 2. أعد تثبيت المكتبات
pnpm install

# 3. جرّب اختبار بسيط
node -e "console.log('Node.js works!')"
```

---

## 📈 تحليل متقدم

### قياس الأداء عبر الزمن

لقياس الأداء على فترات:

```bash
# اختبار كل ساعة لمدة يوم
for i in {1..24}; do
  echo "=== الاختبار رقم $i ==="
  npm run test:concurrent >> test_results_$(date +%Y%m%d).log
  sleep 3600  # ساعة
done
```

### اختبار الضغط الشديد

لاختبار أقصى قدرة للنظام، عدّل السكريبت:

```javascript
// في testConcurrentAccess.js
// غيّر الأرقام:
await testConcurrentAccess(50);   // 50 جهاز
await testConcurrentAccess(100);  // 100 جهاز
```

---

## 🎓 نصائح للاختبار

### 1. الوقت المناسب للاختبار

- ✅ **قبل إطلاق النظام** - للتأكد من الجاهزية
- ✅ **بعد التحديثات الكبيرة** - للتأكد من عدم كسر الوظائف
- ✅ **دورياً** - للتأكد من استقرار النظام

### 2. البيئات المختلفة

اختبر على:
- ✅ شبكات مختلفة (WiFi, 4G, 5G)
- ✅ أجهزة مختلفة (كمبيوتر، جوال، تابلت)
- ✅ متصفحات مختلفة (Chrome, Safari, Firefox)

### 3. توثيق النتائج

احتفظ بسجل للنتائج:
```bash
npm run test:all > test_results_$(date +%Y%m%d_%H%M%S).log
```

---

## 📞 الدعم

إذا واجهت أي مشاكل:

1. راجع ملف `CONCURRENT_ACCESS_TEST_REPORT.md`
2. تحقق من Firebase Console
3. راجع `SYSTEM_STATUS_REPORT.md`

---

## ✅ قائمة التحقق النهائية

قبل اعتبار النظام جاهزاً، تأكد من:

- [ ] جميع اختبارات الدخول المتزامن نجحت (100%)
- [ ] اختبار Firestore للقراءة نجح (100%)
- [ ] جميع الملفات الثابتة تُحمّل بنجاح
- [ ] معدل الاستجابة < 1000ms
- [ ] قواعد الأمان تعمل بشكل صحيح
- [ ] لا توجد أخطاء في Console
- [ ] الموقع يعمل على جميع المتصفحات
- [ ] قاعدة البيانات تُحدّث بشكل صحيح

---

**آخر تحديث:** 7 فبراير 2026  
**الإصدار:** 1.0.0  
**الحالة:** ✅ موثق بالكامل
